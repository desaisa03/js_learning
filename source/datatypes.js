// NOTE: in JS the variable (container) does not have a datatype of its own
// only the value stored in the variable has its own datatype
// so we can change a variable to be a different datatype along the way.

/*
let x = 5;
console.log (typeof x);
x = 'test';
console.log (typeof x);
*/

// regardless of whether we are using a integer or decimal 
// the type is stil number

/*
let b = 1.1;
console.log(typeof b);
*/

// also if you just declare a variable and then do not have anything assigned to it then it is of
// data type 'undefined'

let m;
console.log(m);